# Development / test dependencies. Install with:
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.6.0
black>=24.0.0
mypy>=1.10.0
pip-audit>=2.7.0       # dependency vulnerability (CVE) scanner — see `make audit`
